rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default deny all
    match /{document=**} {
      allow read, write: if false;
    }

    // User data subcollections isolated by path UID
    match /users/{uid}/{document=**} {
      allow read, write: if true;
    }
    
    // Public proposals viewable by public token
    match /publicProposals/{token} {
      allow read, write: if true;
    }

    // Secure OAuth tokens readable only server-side
    match /secure_connections/{doc} {
      allow read, write: if false;
    }
  }
}

